[package]
  name = "rust-cli-sysinfo"
  version = "0.1.0"
  edition = "2024"

[dependencies]
  pci-ids = "0.2.6"

[lints.clippy]
  all = "warn"                  # all lint group includes correctness, suspicious, style, complexity, and perf
  pedantic = "warn"
  cognitive_complexity = "warn" # part of restriction lint group